version: "3.9"
services:
  prediction:
    build: .
    command: python app.py
    image: cifar-prediction-service:latest
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      FLASK_DEBUG: 1
    volumes:
      - ./src:/home/src/
      - ./resources:/home/resources/
